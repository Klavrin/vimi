import styled from 'styled-components';

const StyledSidebarInteractiveZone = styled.div`
  width: 5px;
  height: 100vh;
  position: fixed;
  left: 0;
`;

export default StyledSidebarInteractiveZone;
