const primaryBackground = '#3c3836';
const primaryText = '#edddaa';

const gruvboxTheme = {
  backgroundColor: primaryBackground,
  color: primaryText,
  borderColor: 'rgba(255, 255, 255, 0.1)',
};

export default gruvboxTheme;
